# Authorization — the reasoning

Governs `lib/authz*.js`, `lib/prVisibility.js`, `lib/invoiceVisibility.js`, `app/api/**`. **Read this before editing there** — CLAUDE.md carries only the rules that bind code outside this area; the derivation, the evidence and the alternatives weighed are here.

Moved verbatim out of CLAUDE.md — nothing in this file was rewritten. The migration was audited line by line and the result is in the pull request that created this file.

### Row visibility — `canViewPR` (lib/prVisibility.js)

In order, first match wins:

1. **A `Draft` is visible only to its Requester** — ahead of everything, including President/Admin (#143). Every other Draft surface is already private that way (the resume prompt and the drafts list both read `getDraftsByRequester`), and nothing in the app links to someone else's Draft. An unsubmitted PR is not yet a record of anything, and its assigned signers have nothing to sign.
2. **President/Admin** see every submitted PR.
3. The **Requester** sees their own.
4. Anyone **assigned to the PR's Job** sees it.
5. A **signer on this PR's chain** sees it (#143).
6. The **recipient of a correction request on this PR** sees it (#143).

Clauses 5 and 6 are not generosity — without them the gate cuts the signing chain. Neither a signer nor a correction recipient is guaranteed to be assigned to the PR's Job, and a participant who cannot open the PR cannot take their turn, so the chain stops with nothing to explain it. Both are **status-agnostic**: a signer who already approved, one pushed back to Pending by a correction, and the recipient of an already-`Resolved` correction all keep access. Being locked out of a document you signed would be strange, and every chain action ends in `redirect()` back to this same page, so access that expired at the moment of acting would bounce the actor into a refusal.

**How 5 and 6 cost no queries.** Both sides of each relation already carry the child row ids as link arrays, so membership is a set intersection: `Users."PR Signers"` × `Purchase Requests."PR Signers"`, and `Users."Correction Requests (Sent To)"` × `Purchase Requests."Correction Requests"`. `recordToUser`/`recordToPR` expose them as `signerRowIds`/`correctionRowIds` — the same reverse-link trick `purchaseOrders` already used, and confirmed on a freshly created row to be populated on both sides immediately, with no propagation lag. So the PR list stays one pass over rows it already has, with **no per-row round trip**, and the detail page adds nothing either. The cost is that an intersection cannot read a child's fields, so clause 6 *cannot* filter on `Status` — which is free here, because status-agnostic is what we want anyway.

**`Sent To` ambiguity does not reach this.** CLAUDE.md records that it holds one user id and cannot say whether a correction went to someone *as a signer* or *as the Requester* when they are both. That is about the recipient's capacity; clause 6 only asks whether they are the recipient at all.

**`Withdrawn` takes no special path** — #122 keeps a withdrawn PR on record and in the list, so it is judged like any other submitted PR.

**A missing link array throws rather than refusing.** Once clauses 1–4 have not decided, the answer depends on `signerRowIds`/`correctionRowIds`, and a caller that omitted them is a bug rather than a denial. Refusing would look like the safe direction and would in fact be the stalled chain 5 and 6 exist to prevent — a signer locked out of the PR they are meant to sign, with no error to trace. Both mappers default the fields to `[]`, so `undefined` can only mean the object never went through one; every production caller does today, which makes the throw unreachable now and a tripwire for the next call site. It fires only where the arrays are actually needed, so a decision already reached by clauses 1–4 still works on an object without them. What it cannot catch: Airtable omits an empty link field, so "no signers" and "field not selected" are indistinguishable downstream — the mappers' `|| []` is what turns both into `[]`, and a field-limited select would pass this check while silently disabling both clauses. No such select produces a `canViewPR` input today (the only ones are in `lib/ids.js`, for sequence numbering).

**Guard wrappers (#147) — the expected way to gate a new endpoint.** `lib/authz.js` exports three: `withAdminApi` (Route Handler; refuses with requireAdminApi()'s own 401/403 Response), `withAdminAction` (Server Action; the call site passes its refusal as the first argument) and `withPresidentAction` (Server Action; requirePresident() throws, so there is nothing to pass). A wrapped export cannot run its body unauthorized, because the body is an argument the wrapper decides whether to call — the caller obligation below stops being a thing anyone can forget. **The refusal shape follows the call site, and #185 settled it** — the decision #147 deferred by keeping refusal as the thunk's argument. **An action whose every call site BINDS the return value returns `{ error }`; an action any call site invokes without binding throws.** Three bindings exist and two of them can observe a return: `useActionState(action, init)` makes it `state`, and `await action(...)` in a client handler makes it a value, while a bare `<form action={action}>` DISCARDS it. That third one is why the rule is not "a form returns" — `/admin/jobs/new` and `/admin/vendors/new` ARE forms and cannot see a returned refusal, because each page is a Server Component handing the action straight to the element with no client boundary and no error slot on the screen at all. **The rule is a conjunction over call sites**, and the disagreement case is part of it: one non-binding caller makes the rule say throw, which costs the binding caller its inline message, so a mixture is the signal to change that caller rather than the shape. `offline/action-refusal-shape.mjs` enumerates every wrapped action, reads each thunk's actual shape off the AST, derives the shape from the call sites and compares — so the rule is applied rather than restated, and a new action fails the tier until it is classified.

- **What the throw was actually carrying, and what took it over.** The undroppability #147 needed is the WRAPPER's, not the throw's: `createFlagGuard` calls the thunk INSTEAD of the handler, so the gate cannot be skipped whatever the thunk returns, and `offline/authz-structure.mjs` holds that unchanged. What a throw really bought was OBSERVABILITY — a refusal a caller ignores still surfaces. Converting to a return gives that up, so the new check takes it: every returning action's every observing call site must have a path from the bound value's `.error` into JSX. That is the quiet mutant this settlement creates and the assertion that answers it.
- **A THROWN REFUSAL IS NOT SCREEN COPY IN THIS APP, and the reason is a premise rather than a preference.** `app/` contains no `error.js` and no `global-error.js`, so nothing in this repository renders a thrown Server Action message; it reaches the framework's own default. So the two throwing actions' text is developer-facing, needs no copy review, and is not a string a screen shows — `scripts/screen-strings.mjs` stopped collecting thrown messages from `"use server"` files on exactly that ground (#185), which took nine strings out of its census that no reader could ever meet. **The day somebody adds an error boundary, those refusals become copy** — `action-refusal-shape.mjs` asserts the absence, so that day it fails and this paragraph is what the failure sends you back to. The count is deliberately not written down here: #272 added an action and made "four and four" false in three places at once, so what splits them is recorded and the arithmetic is left to the check.
- **The rule does not reach `withPresidentAction`.** `requirePresident()` throws inside the gate, so there is no refusal argument for a call site to supply and nothing for the rule to decide.

None of the three passes the resolved user to the handler, because no Admin/President call site reads one — every requireAdmin() site destructured only `{ authorized }` and every requireAdminApi() site used its value only for the `instanceof Response` test. Add it as a first argument if a wrapped handler ever needs it.

**Not wrapped, and why (#147).** These are the exemptions, listed with reasons in `scripts/tests/offline/authz-structure.mjs` and enforced there. The list is meant to stay short for two reasons: every entry is a precedent a future author can copy, and an exemption is verified more weakly than a wrapper (see the scope note below).
- The three magic-link auth routes — public by design; there is no caller to authorize.
- `/api/invoices/upload`, `/api/quotations/upload` and `/api/deliveries/upload` — the gate has to run inside `handleUpload`'s `onBeforeGenerateToken` callback, which rejects by throwing rather than by returning a Response, so wrapping the export would answer 401/403 where the client currently gets the 400 `handleUpload`'s catch produces. Recorded because #147 found it: `request.json()` runs at the top of all three handlers **before** authorization, so "the gate comes before the route's work" is looser here than the phrase suggests. These three are the only places in the inventory where that is true, and the shape propagates — the deliveries route (#162) inherited it by being the same kind of endpoint, which is exactly what the note about every exemption being a precedent predicted.
  - The deliveries route carries one more looseness of its own, named in its exemption: it gates on `getActiveUser()` and **cannot** check the Job, because the upload happens before the form is submitted so the route does not yet know which Job the photo belongs to. The Job check is in `createDeliveryAction`. So any active user can put an object in the store; what they cannot do is attach it to a delivery.
- `GET /api/files/[axis]/[documentId]/[filename]` (#331) — session plus one of THREE per-record gates, chosen by the axis segment. `canViewPR` for a quotation and an order document, both reached through the parent request; `getVisibleInvoiceIds` for an invoice file; `canAccessJobDeliveries` for a packing list photo and a direct purchase's file. Each is the gate that axis's own screen already uses, so the route adds no visibility rule. No wrapper fits: `withAdminApi` would refuse the site staff every one of those gates admits, and Route Handlers cannot use `requireUser()` at all.
  - **THIS IS THE ONE EXEMPTION WHOSE WEAKNESS IS ANSWERED IN ANOTHER FILE RATHER THAN TOLERATED HERE, and the reason is arithmetic.** Every other entry is one gate a wrapper does not fit. This is five fields behind three gates in one export, so "the named helper is called somewhere inside" — which is the whole of what an exemption proves, order not checked — is satisfied exactly as well by `getActiveUser()` plus ONE gate for all five. No screen would change, nothing here would fail, and a reader would open a packing list for a job they are not on because the handler asked `canViewPR` about a request they happen to be a signer of. `offline/file-route.mjs` holds it instead: the route declares each axis's gate in a map AND calls a gate inside each axis's own opener, in two places on purpose, and that check reads both off the AST and requires them to agree with each other and with its own table. Verified by mutation — a swapped declaration, a swapped call with the declaration left right, one gate for all five, and an opener pointed at `Purchase Orders."Quotation File"` all fail it.
  - **The refusal is one answer for two states**, `404 Not found.` for a record that does not exist and for one outside the reader's scope alike, which is CLAUDE.md's rule about never confirming existence. It carries more weight here than on a page: the URL holds a human document id whose neighbors a person can guess, so the indistinguishability is what makes enumeration learn nothing. Demonstrated with a real session — `authz-fixture@` (non-Admin, no assigned jobs) gets that body on all five axes, on two ids that do not exist, and on an unknown axis. `scoped-fixture@` gets 200 on the order document, the invoice file and the packing list photo, and **404 on the quotation**, because the request behind it is a Draft and `canViewPR`'s first clause admits only its requester — the same gate admitting and refusing one reader through one route.
- `createOverageDraftAction` (#167) — `requireUser()` plus the Job check, the same per-record axis as the other delivery actions, and **deliberately not Admin**: raising the correction is site work. It is the exception to #166's withholding described under "Overage corrections", and it re-derives eligibility from a fresh read so a correction raised in another tab lands as a refusal rather than a second Draft.
- The eight `requireUser()` Server Actions — session + per-record/per-turn ownership, not a role. `requireUser()` already cannot be dropped (it redirects), and the actual authorization is the record-by-record comparison in the body. A wrapper here would cover the half that was never at risk and leave the deciding half uncovered, while looking like coverage.

**Caller obligation:** requireRole()/requireAdmin() only *report* the decision — a caller must destructure the returned `{ authorized }` and short-circuit on false (throw, return `{ error }`, or render a refusal). Calling one without acting on the flag protects nothing (the code runs on). requirePresident() and requireAdminApi() are both preferred over the flag helpers for the cases that use them, but they are not equally undroppable, and the difference is the return: requirePresident() *throws*, so control cannot continue past a refusal and there is nothing for a caller to ignore; requireAdminApi() *returns* the 401/403 Response, which is a value the handler has to return for anything to be blocked — the call on its own refuses nothing, so leaving its result unassigned is still a way to lose the gate. What it removes versus `{ authorized }` is the flag to misread, not the caller's obligation.

**Re-authorization rule (#134):** every directly-callable endpoint re-authorizes to the level of the strictest page that renders its UI — a page being the only caller isn't a substitute, since Route Handlers and Server Actions are reachable directly. Server Actions use the wrappers above (signPOAction/regeneratePDFAction are `withPresidentAction`; generatePOAction, the PO-generation retry, is `withAdminAction` with a matching isAdmin render gate on app/prs/[prId]). withdrawPOAction (#138) is the per-record variant: requireUser() only settles "is this an active session", and the actual authorization is an ownership comparison against the parent PR's Requester inside lib/poWithdraw.js — no role helper fits, since the requester is typically neither President nor Admin. `/api/*`: the magic-link auth routes are intentionally public, /api/quotations/upload is any-active-user (matching the PR form that consumes it); /api/invoices/upload, /api/invoices/detect-po, /api/pos/search, /api/pos/[poRecordId]/items are Admin-only via requireAdminApi() (their sole consumer is the Admin-only invoice form). Any route that fetches a caller-supplied URL also restricts it to our Vercel Blob host (detect-po's SSRF guard), independent of auth.

**What enforces the rule (#147).** `scripts/tests/offline/authz-structure.mjs` is a structural check over an **enumerated** inventory — enumerated because a hard-coded path list is blind to any route added later: it walks every `app/api/**/route.js` and every `"use server"` file under `app/` and `lib/`, parses each with acorn (so a comment can satisfy nothing), and requires each export to be either wrapped by one of the three wrappers or listed as an exemption with a reason. It also flags a function-level `"use server"` directive anywhere in those trees — an inline Server Action is directly callable too, and none exist today, so one appearing should fail rather than pass by not being looked for. A file it cannot parse fails; an unparsed file is an unchecked file. A stale exemption (the export is now wrapped, or no longer exists) fails too, so the list can't rot into a blanket permission.

**What a pass proves, per case.** For a **wrapped** export: the gate cannot be skipped and cannot run late, because the handler is an argument the wrapper decides whether to call — ordering is structural, so there is nothing left for the check to assert or for an author to get wrong. For an **exempt** export: only that the named helper is called somewhere inside the exported function. **Order is not checked** — the old check's `gateIdx < workIdx` comparison has no counterpart, so an exempt route that does work before its gate still passes. That is not hypothetical: `/api/invoices/upload` and `/api/quotations/upload` both call `request.json()` before authorizing, which is why that fact is written into their exemption reasons. This is the check's exact scope rather than a defect in it, and it is the reason an exemption is worth less than a wrapper.

**It is a standing check, not issue evidence.** It runs with no env vars, no Airtable, no dev server and no module loader, writes nothing, and exits non-zero when it fails — so since #152 it sits in the offline tier that `npm test` and CI run on every push (see "Verification tiers" below). `scripts/tests/verify-authz.mjs` keeps only what genuinely needs credentials: the Blob host predicate, PO-generation idempotency against a throwaway PR+PO, and real HTTP 401/403 refusals. That one needs `.env.local`, the loader, and a dev server, and it exits 2 when a part could not run so an incomplete run never reads as a pass. Adding an endpoint without a gate fails the offline tier without anyone remembering to register it, which is the property the old check didn't have.

---

### Invoice visibility (#211)

**The two invoice routes were President-or-Admin and NO REASON FOR THAT WAS EVER RECORDED.** #132's "the invoice pages stay President-or-Admin" scoped *that* issue rather than deciding this one, and #166 withheld vendor invoice figures from the delivery screens without saying why either. So there was no argument to overturn, and the reason this file now carries is the pair that replaced it.

- **The boundary was already leaking.** #167 hands a site employee the vendor's own invoice PDF as the quotation on a corrective request and puts its number in `Vendor Quotation Code`; `/pos/[poId]` shows that same employee the `Amount` column. What the company agreed to pay was fully in view while what the vendor charged was not — not a line anyone would draw on purpose.
- **The person who counted the material is the only reader positioned to notice that a vendor invoiced for thirteen and shipped ten.** Withholding the invoiced quantity from them removes the one check that catches it.

**The rule is `canViewPR`, reached through the order.** President and Admin see every invoice; anyone else sees one whose items sit on a PO whose parent PR they raised or whose parent PR sits on one of their assigned Jobs. `lib/invoiceVisibility.js` owns the WALK and no predicate of its own — `offline/invoice-visibility.mjs` asserts on the AST that it imports and calls `canViewPR` and that it touches none of that function's own inputs, because a second predicate would not fail anything for as long as the two happened to agree.
- **ANY line is enough.** A multi-PO invoice is real, so one document can charge an order the viewer raised and one they have never heard of. The viewer is looking for the line that is theirs, and refusing the whole document because it also covers someone else's order would hide the thing they can actually check.
- **An invoice with no resolvable order is REFUSED**, which is the opposite direction from `canViewPR`'s own throw-on-missing-data — there, refusing would stall a signing chain; here it just means an employee does not see a document, which is the pre-#211 state. Measured on this base: `scoped-fixture@` sees fewer invoices than an Admin, and the excluded ones were the hand-entered dummies with no `PO` link (`HYE-INV-260727-03` rendered the `None linked.` empty state, which #278 removed along with the state that reached it — the refusal itself is now a fail-closed default rather than a described case).
- **Two operations for a page of any size, and ZERO for the office WHERE THE VERDICT IS ALL THAT IS WANTED** — the privileged answer needs no `canViewPR` call, so `getVisibleInvoiceIds` returns it without reading anything. **#314 narrowed that sentence**, which used to state it of the walk itself: `resolveInvoiceScope` is the same walk with its orders and requests handed back, and it walks whoever is reading, because the invoice list needs a request's `Job` for its own column and resolving a rendered fact from a walk one reader skips is a column whose value depends on who is looking at it. So the list costs 14 operations for the office and 14 for a site employee, up from 12 and down from 15 — one budget, which is what #309 gave the columns. The other three call sites take the verdict-only shape and are unchanged. The list still runs the reconciliation over the GATED rows, so a refused invoice's items never reach the wire either (#169's shape).

### Payment was President-or-Admin (#211) and is open to every reader of the row (#309)

**#211's line was drawn deliberately rather than inherited**, and the reason it gave was that whether a vendor has been paid is the one fact on these screens a recorder has no use for and a vendor's own staff might ask about on site. **The office asked for the opposite**, so the line is reversed: the gate deciding whether a document is visible is the only gate on reading it, and payment carries none of its own. Recording it did not move — the checkbox and the date are still President-or-Admin, which is the split #211 never had to make while reading and writing stood behind one answer.

**THAT SPLIT IS THE WHOLE OF THE RISK, AND IT IS STRUCTURAL RATHER THAN WRITTEN DOWN.** `seesEveryInvoice` answered two questions — may the walk be skipped, and may the payment line render — so the read gate and the write gate were one nesting apart with one predicate behind both. Collapse them and opening the read opens the control, with every check still green. `offline/invoice-visibility.mjs` assertion 3 is the answer: **a branch whose test asks a PRIVILEGE question may not have the payment fact on one side only.** `{privileged && <Payment/>}` fails; a ternary stating the fact on both sides passes, because a reader who cannot record it still reads it. **#318 left that screen with no such branch at all** — the section states the fact unconditionally and hands the privilege answer to its Client Component as `canEdit` — and added assertion 3b, which runs the same rule over branches on the section's open/closed state, where the identical mutant can now be written. Assertion 4 keeps the FILE inventory (`PAID_READERS`) — a new reader of `.paid` fails CI until registered — and the two are needed together: a file list cannot see one screen left gated, which is exactly what `/pos/[poId]` was.

| Where | What #211 did | What #309 did |
|---|---|---|
| `/invoices` Status column | rendered only for a privileged viewer | **open, and the header variance badge inside it with the column.** #179 sent that badge behind the payment gate because only an Admin can act on it; the same fact is stated ungated one click away, in the red box on the invoice's own page with both figures, which #179 itself relied on when it narrowed the amber prompt. A mark on the row a reader clicks and no mark on the page they land on was the inconsistency |
| `/invoices` column budget | a SECOND budget: six columns for an employee, seven for the office, the freed 11rem going to Vendor | **one budget again**, Vendor back to 8rem. No screen in this app now drops a column by reader |
| `/invoices/[invoiceId]` Payment section | the whole section, heading included. A heading with nothing under it announces a fact and refuses to say it | **open.** That reasoning was sound and its subject is gone. The `user.isAdmin` branch inside is untouched, and it is where the read and the write now visibly differ |
| `/invoices/[invoiceId]` read-only sentence | `Paid on {date}` / `Not paid yet.`, for a President who is not an Admin | **the ordinary case for every reader since #318.** This base has no President who is not an Admin, so before #309 it rendered for nobody, and until #318 an Admin still never saw it — the sentence was the alternate of a branch whose consequent was the form. That branch is gone: the section states the fact once and only the CONTROL is Admin's — see the note below |
| `/invoices/[invoiceId]` variance prompt | **hoisted OUT of that section** so it outlives the gate | stays out. The gate it had to outlive is gone; the placement is still right, because an item against its order is not a payment fact |
| `/pos/[poId]` `✓ Paid` badge | nothing — already inside #132's `isPrivileged` branch, then its own `seesPayment` flag in #235 | **open**, and `seesPayment` deleted. Every invoice in that section charges the order the reader was admitted to, so `getVisibleInvoiceIds` admits every one of them by construction |
| `lib/deliveryDelete.js` third voice | **was leaking**, and `seesPayment` gated it | **offered to whoever may delete**, and the parameter is gone. The delivery detail's own header carries the derivation: anyone who passes `canAccessJobDeliveries` passes `canViewPR` for every request behind every row |
| `lib/overagePR.js` invoice facts | **a dead `paid` field, removed** | unchanged |

**#211's `/invoices` row above said the column "becomes `Variance` for everyone else, keeping the badge", and that has been false since #179**, which found the badge to be the header kind and sent it behind the payment gate. `naming.md`'s corresponding row was corrected per #181 by #260 and this file was missed by that sweep; corrected here.

**WHAT IT COSTS, IN ONE PLACE.** Four of the five surfaces move by zero operations — payment is on the invoice record either way, there is one mapper with no projection, and #211/#235 had already stopped withholding at the fetch. The exception is `lib/deliveryDelete.js`, whose escalation to the `paid` voice was lazy AND gated, so it stopped one read earlier as well as one voice earlier. A non-office reader now pays what the office always paid: one `Invoice Items` read per invoiced ordered item plus one `Invoices` read per distinct invoice. It lands only where three things hold at once — the reader is not the office, they recorded the delivery themselves (or `mayDelete` is false and the function is never called), and an ordered item is invoiced.

**`seesEveryInvoice` HAS ONE JOB AGAIN, and its own docstring had the count wrong.** It said "three places ask — the list, the detail, and the payment line"; the sites were five, and the third named was not a site but the second question the first two were carrying. Four remain and every one of them is a cost decision: the walk's own short-circuit, the invoice list's `Invoice Items` fetch, and `checkInvoicePairing`'s read and guard. **No new predicate was added** — the alternative, keeping #179's badge gate, needed a fourth inline copy of `President || isAdmin` or an extraction this issue did not authorize.

**AND #314 TOOK THE INVOICE LIST OFF THAT LIST, WHICH IS WHY THE COUNT IS NOT WRITTEN AGAIN HERE.** Its `Invoice Items` fetch had no decision left to make — the `Job` column needs the walk's records whoever is reading — so `app/invoices/page.js` names the helper nowhere and the screen asks no privilege question at all. What replaced that site is inside the module: `resolveInvoiceScope` asks the same thing the list gave up, whether the verdict can be reached without `canViewPR`. **`offline/invoice-visibility.mjs`'s assertion was INVERTED rather than deleted** — it required the list to call the helper, and it now requires that NEITHER invoice route asks who the reader is, with the module's own call sites as the anti-vacuity. The arithmetic is left to the check for the reason #272's own count records: a number in three places goes false in three places at once.

**Both writes are unchanged:** `updatePaidAction` is still `withAdminAction`, and recording or editing an invoice is still Admin-only.

## Auth (lib/auth.js, lib/session.js, lib/email.js, lib/authz.js)

- Magic link only. requestMagicLink() domain-checks then emails a link; verifyMagicLink() consumes the token (withKeyLock-protected), finds-or-creates the User.
- **THE LOCAL PART OF AN ADDRESS IDENTIFIES A PERSON ONLY BECAUSE ONE DOMAIN IS ADMITTED, AND A SECOND DOMAIN WOULD BREAK THAT SILENTLY.** `ALLOWED_EMAIL_DOMAIN` is one variable holding one value and `isAllowedEmail` is a single `endsWith` against it, so no two users can share a local part. Anything that shortens an address to name a person stops identifying the moment that variable becomes a list, because `chkim@a` and `chkim@b` both read as `chkim` — and **nothing in the code would say so**, since widening the check is a small edit in this file's area while the screens that shortened an address are elsewhere. Recorded here rather than beside any such screen, because the constraint is this module's and the screens only inherit it.
  - **And `Users."User Name"` is not that derivation, though it equals it today.** `createUser` is called with `email.split("@")[0]`, `"User Name"` is written in exactly one place — `lib/airtable/users.js` — reached only from there, and no in-app path edits it, so the two coincide as a property of this signup call and **not as anything the schema guarantees**. An Airtable hand edit moves one without the other, which is the point rather than a hazard: a screen reading `User Name` picks up a real display name the day somebody types one, and `lib/prWait.js`'s header is where that was settled for every other screen that prints a person.
- **THE LINK POINTS AT A PAGE, AND OPENING IT CONSUMES NOTHING (#203).** `/login/confirm?token=…` reads the row with `getAuthTokenRecord` — a `select`, no write — and either offers a `Confirm sign-in` button or says why it cannot; `POST /api/auth/verify`, a plain HTML form with no JavaScript, is the only thing that spends the token. **The reason is measured on a real recipient, not hypothesized:** mail security scanners open links in delivered messages before the recipient does, and while the route consumed on `GET` the scanner spent the single-use token well inside its 15-minute window, so the recipient's own click got the invalid-or-expired error. TTL is unchanged at 15 minutes and the single-use guarantee is unchanged — a second POST is refused.
  - **The validity rule left `consumeAuthToken` for `lib/authTokenState.js`**, because the page must reach the same verdict without consuming and three `if`s in two places is one judgment with two implementations. That module owns the five states (`valid` / `missing` / `invalid` / `used` / `expired`), their copy, and `TOKEN_TTL_MINUTES` — which was a private constant beside three prose copies of "15 minutes". `consumeAuthToken` still owns everything the verdict is not: the lock, the read, the write, and the collapse of every refusal into one `null`, since a consumer has no use for WHICH way a token was unusable while a page rendering a message does.
  - **An unreadable `Expires At` is now EXPIRED rather than eternal**, a deliberate tightening: the old comparison was `new Date(x).getTime() < Date.now()` and `NaN < n` is false, so a blank or malformed value — one Airtable UI click away — made a credential that never expired. The boundary itself is unchanged (`expiresAt < now`, so the exact instant is still valid).
  - **The POST refuses a cross-origin submission, and the token does not make that redundant.** The token authenticates the request but not the submitter's intent, so an attacker can POST their OWN token through a victim's browser and land the victim in the attacker's account — login CSRF. That is not a cosmetic mislabeling here: the signing chain rests on who submitted a PR, so a victim authoring under another identity corrupts the record this app exists to keep. `sameSite: "lax"` does not help, since it governs a cookie being SENT and this response SETS one. The check compares `Origin` against `Host` (the public host behind Vercel's proxy, unlike `request.url`) and **fails open when `Origin` is absent** — every current browser sends it on a form POST, and refusing on absence would break odd clients while stopping no attacker, who can set any header. A malformed `Origin` is a rejection: it is present, so the fail-open case does not apply.
  - **The confirmation page shows the target address**, which withholds nothing — anyone who can read the page already holds the token and can press the button to become that user. It is what makes a login-CSRF attempt visible to its victim, since being asked to sign in as somebody else is the one thing that reads as wrong.
  - **`app/login/page.js`'s `?error=` messages are gone**: their only two producers were the redirects in the old `GET` handler, so with that handler gone nothing could reach them. A refusal now returns to `/login/confirm` carrying the same token, and the page names the actual reason instead of a generic one. This also removed the `useSearchParams` call and the `Suspense` boundary that call required.
  - **Two checks, proving different things.** `offline/auth-token-state.mjs` pins the five verdicts, the exclusive expiry boundary and the copy; `offline/source-shape.mjs` pins that the confirmation page never calls or imports `consumeAuthToken`/`verifyMagicLink` while still calling `getAuthTokenRecord` — that last clause is the anti-vacuity, since two absences are also what a wrong path or a failed parse reports. The import half reads the AST rather than the file text, because the page's own doc comment names both functions while explaining why it must not call them; prose about a rule is not a violation of it. **Neither check can see a mail scanner** — what they establish is that a `GET` leaves the row unconsumed.
- lib/session.js: iron-session, payload `{ userId }`. getCurrentUser() treats a missing Users record as logged-out, re-throws real Airtable errors.
- getActiveUser() (lib/authz.js) also treats Status: Inactive as logged-out.
- requireUser()/requireRole(role)/requireAdmin()/requirePresident(): Server Component/Action helpers. Failure modes differ: all four redirect to /login on no session; on insufficient permission requireRole/requireAdmin return `{ authorized: false }` (caller renders inline), while requirePresident() throws (its PO-signing callers have no per-branch UI). Route Handlers can't use these (redirect() is for the page-render pipeline) — they call getActiveUser() (any active user) or requireAdminApi() (Admin-only), which return the user or a 401/403 JSON Response.
- No proxy.js/middleware — each page's own requireUser() call is the gate.
- Env vars: SESSION_SECRET, RESEND_API_KEY, ALLOWED_EMAIL_DOMAIN, EMAIL_FROM (optional). Fail-fast at module load; set in Vercel too. **Only `RESEND_API_KEY` is fail-fast** — `EMAIL_FROM` falls back to Resend's sandbox address carrying the product name as its display name (`lib/email.js:FROM_ADDRESS`), and whether the optional one should become required is a change to this module's failure mode rather than a naming question, so #201 left it alone deliberately.
- **Resend's domain is verified — mail delivers to any address.** It was sandbox-only (account owner's address only) for most of this project's life, which is why several decisions here are written against that constraint; the constraint is gone, but the decisions that cite it were made under it and are not retroactively wrong.
- **The product is named in one place: `lib/productName.js` (#201).** `PRODUCT_NAME` is `HYE USA Portal`; `SIGN_IN_TITLE` is the one sentence carried by both the magic-link email's subject and `/login`'s `<h1>`, which is what stops those two from drifting. **Not the company's legal name** — `lib/poPdf.js:HYE_BUYER_NAME` (`HANYANGENG USA INC.`) is what a vendor sees on the purchase order, and the two have different owners and must never move together. Its own module rather than a constant in `lib/email.js` because `app/login/page.js` is a Client Component and `lib/email.js` throws at module load without `RESEND_API_KEY` — #162's trap, an import is an execution.
  - **The superseded names, recorded here because this is the only place allowed to spell them.** They are Material Workflow Automation (CLAUDE.md's own former title) and Material PO Automation (the title of the build plan this repo carried until it was deleted as stale, and the name in every email — written out four times in `lib/email.js` alone). Deliberately **without backticks**, the same convention the file-rename paragraph uses, so a later sweep for either string leaves this paragraph alone: rewriting it would delete the record of the work while appearing to do it. That two documents disagreed is the evidence for the issue's diagnosis — nothing owned the string, so nothing kept them together.
  - **`offline/product-name.mjs` is why the names cannot be spelled in the code**, including in a comment: it fails on either of them appearing anywhere under `app/` or `lib/`, and it also fails when `PRODUCT_NAME`'s VALUE appears as a literal in any file but its own — because "the old name is gone" is equally satisfied by writing the new one out at seven sites, which is the state that let them drift. **It carries no exemption list, and the `app/` + `lib/` scope is what buys that** (the same boundary `offline/formula-escaping.mjs` draws): every place that legitimately has to spell an old name — this file, a commit message — is outside the scope, so none of them needs excusing. #171 records how quickly an exemption list rots and #174 records a blanket ban blocking a legitimate use; a scope making both unnecessary beats either. Verified by mutation on four branches, and it earned its place immediately by catching a comment in `app/layout.js` that spelled the new name.

---
